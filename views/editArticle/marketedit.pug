extends ../articleLayout

block content
    #main
        #board
            #title.article
                h1
                    a(href='/market') 장터
            #writeArticle
                form.write(method="post")
                    p 
                        input(name="title"  class="title" value=row.m_title)
                    p 
                        //- input(name="text" class="smallplaceholder large"  value=row.m_txt)
                        textarea(name="text" class="smallplaceholder large"  placeholder=row.m_txt)
                    input(type="submit").submit
